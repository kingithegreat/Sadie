name: Release Gate

on:
  # Human pushes to main still fire this. But the auto-merge bot squash-merges
  # with the repository's GITHUB_TOKEN, and GitHub does NOT fire push events for
  # GITHUB_TOKEN pushes — so a push-only trigger left this gate dark on main for
  # 30 consecutive merges (2026-09-16 -> 2026-09-18, commits 457bd92 back to the
  # last real run 35079766730) while still looking healthy on its own PR branch.
  # The schedule and workflow_dispatch below are what actually cover bot merges.
  push:
    branches:
      - main
  # Daily check that main stays releasable. Scheduled workflows run on the
  # default branch, so this fires regardless of who merged. 06:00 UTC is 4h
  # after the Nightly Media E2E (02:00 UTC) so the two heavy Windows jobs never
  # compete for runner capacity.
  schedule:
    - cron: '0 6 * * *'
  # Release time: prove the current main is releasable on demand.
  # `gh workflow run release-gate.yml --ref main`.
  workflow_dispatch:
  # Changes to this gate prove themselves on their own PR before they reach main.
  pull_request:
    paths:
      - .github/workflows/release-gate.yml

# Two full widget suites on one Windows runner at once is what made the
# storyboard-renderer timeout flake, so never let this gate stack on itself.
concurrency:
  group: release-gate-${{ github.ref }}
  cancel-in-progress: true

# Windows, matching ci.yml's `widget` job: HomeBot ships Windows-only, and the
# widget suite is written for it (fixtures live in os.tmpdir(), which the
# main-process tools accept only inside the home directory; tests use
# C:\Windows paths as "outside your folder"). On ubuntu-latest this gate failed
# every main run from at least 2026-09-13 to 2026-09-16 with 122 tests in 21
# suites red for those reasons alone, so nobody could read it as a release signal.
jobs:
  build:
    name: Build
    runs-on: windows-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: widget/package-lock.json
      - name: Install (root)
        run: npm ci --ignore-scripts
      - name: Install (widget)
        working-directory: widget
        run: npm ci --ignore-scripts
      - name: Build widget
        working-directory: widget
        run: npm run build

  unit-tests:
    name: Unit Tests
    runs-on: windows-latest
    needs: build
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: widget/package-lock.json
      # Same install as ci.yml `widget`; see the comments there for why each step exists.
      - name: Install (root)
        run: npm ci --ignore-scripts
      - name: Install (widget)
        working-directory: widget
        run: npm ci --ignore-scripts
      - name: Build native modules against Node
        run: |
          npm rebuild better-sqlite3
          cd widget && npm rebuild better-sqlite3
      - name: Point TEMP inside the home directory
        shell: pwsh
        run: |
          $tmp = Join-Path $env:USERPROFILE 'homebot-test-tmp'
          New-Item -ItemType Directory -Force -Path $tmp | Out-Null
          "TEMP=$tmp" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
          "TMP=$tmp"  | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
      - name: Run unit tests
        working-directory: widget
        run: npx jest --config=jest.config.ts --runInBand --no-coverage --forceExit

  e2e-smoke:
    name: E2E (smoke)
    needs: unit-tests
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: widget/package-lock.json
      - name: Install widget deps
        run: npm ci --prefix widget
      - name: Build widget
        run: npm run build --prefix widget
      - name: Install Playwright browsers
        working-directory: widget
        run: npx playwright install
      # Named specs, not `-g "smoke"`: no test title contains "smoke", so that
      # filter selected 0 tests and Playwright failed with "No tests found".
      # These two launch the built app: its UI draws, and Studio creates a real
      # project through its IPC and Core tool executor.
      - name: Run smoke E2E
        working-directory: widget
        run: npx playwright test src/renderer/e2e/brand-and-icons.e2e.spec.ts src/renderer/e2e/studio-host.e2e.spec.ts --retries=2 --trace on-first-retry --reporter=line
      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: release-gate-artifacts-${{ github.run_id }}
          path: |
            widget/playwright-report/
            widget/test-results/
            widget/traces/
