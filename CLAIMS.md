# Work Claims

Repo-native companion to the Notion [ðŸ”— Work Claims Ledger](https://app.notion.com/p/63dec393c18546ee9b924a472e9c59a3) and [ðŸ¤� Coordination Protocol](https://app.notion.com/p/390829ebf7be81e09866e1cd3a6bed6e). This file exists because Notion is easy to skip; this file is not â€” anyone building here sees it just by looking at the repo.

**Before starting a new feature build:**
1. Check the table below for an existing claim on the same feature area. If one exists and looks recent/active, don't start a parallel build.
2. `grep -rl <concept> src widget/src` for an existing module before writing a new one — HomeBot has two source trees (repo-root `src/` for pure/testable modules, `widget/src/` for the actual Electron app), so check both.
3. Add a row below before writing code. Update it to "Ready for Integration" when pushed and self-tested — and **strike it into the retired table the moment it merges**. A row's last edit is normally the one that makes it a lie: on 2026-08-26 every one of the nine "active" rows described merged work, because writing a row is part of starting and nobody owns finishing. If you merge a PR, close its row in the same breath; if you find a row whose PR has merged, retire it there and then rather than leaving it for whoever reads next.
4. **Verify a claim against the repo before repeating it** — `gh pr view <n> --json state,mergedAt` is two seconds. The board is a lagging record, not a source of truth: on 2026-08-24 two rows said "Ready for integration" for PRs that had merged 48 hours earlier, and both got repeated as fact. Note that squash-merge means the original branch SHA is never an ancestor of main, so `git branch --contains` lies — check PR **state**, not commits. **`gh pr list --head <branch>` lies too**, in the opposite direction: it returned "no PR" for `claude/dead-capability-sweep-rebased`, whose content had merged as #118 and is on main today — the PR had been opened from the pre-rebase branch name, so the lookup found nothing and the branch looked like stranded work waiting to be rescued. And do not trust a commit SUBJECT either: #228 reads `docs(claims):` but carries the entire Track H file-trigger feature across nine files. When the question is "did this ship?", grep main for the CONTENT.
5. **Each session works in its own git worktree. `C:\Users\adenk\Desktop\sadie` is nobody's checkout.** Two sessions sharing one HEAD interleaved reflog entries, reset a branch underneath a running merge, and clobbered an uncommitted claim row — three losses on 2026-08-24, one costing a full rebuild, all tracing to this. Aden provisions per-session worktrees (e.g. `Desktop\sadie-ox`, with `node_modules` junctioned so the widget suite runs immediately); ask for yours instead of touching the shared tree. Never run mutating git commands against another session's worktree — `git worktree list` first, every time. Happened again on 2026-08-26: a session working directly in Desktop/sadie lost thirteen files of uncommitted edits when another session moved that tree's HEAD mid-task, and had to rebuild them from scratch in its own worktree.

CI runs `scripts/check-duplicate-exports.mjs` on every PR and will fail the build if a newly added file exports the same top-level identifier as an existing file â€” usually a sign two features were built in parallel without checking here first, or a scaffold was built without checking the real integration point (see the Step 1 entitlements gate channel-name mismatch, which was a variant of this same failure mode). See that script's header comment for how it works and how to suppress a genuine false positive.

## Active claims

| Feature | Branch | Status | Notes |
|---|---|---|---|
| Task 3 — reliability gates for the real Media Studio "Make the video" pipeline | claude/task3-media-qa-reliability-v2 | Ready for integration (2026-09-12) | Rebase of `claude/task3-media-qa-reliability` (PR #293) onto fresh main after #290/#291 merged — `git merge-tree` confirmed the only real conflict was this CLAIMS.md row-insertion point; code applied cleanly, tsc/eslint/full-suite re-verified identical. Scoped narrowly to the one real, job-based pipeline (`media_narrate`/`media_render` in `tools/media.ts` → `media-render.ts` → `media-qa.ts`) that's actually wired to the UI's "Make the video" button — NOT the movie/project-runner image pipeline, the Ancient Pathways Python bridge, or the storyboard renderer, which remain separate, disconnected pipelines, explicitly left out of scope rather than silently declared fixed. Fixes: (1) Kokoro-engine narration duration was computed via a hardcoded 96kbit/s MP3 bitrate assumption (`mp3DurationSeconds`) even though Kokoro writes WAV at ~4x that bitrate, so every Kokoro job failed the real duration-drift QA check against its own correct render — fixed by measuring the real file via the existing `inspectRender`; (2) `media-qa.ts` had no visible-content check — a solid-color placeholder video passed every existing gate — added real ffmpeg frame-sampling + pixel-variance (`sampleFrameVariance`/`frameStdDev`), chosen over `blackdetect`/`freezedetect` because this renderer's placeholder colors are dark slate (not black) and the default `zoompan` Ken Burns effect defeats freeze detection on a flat source; (3) captions were generated and burned in but never verified non-empty/duration-matched — added, reusing the existing `parseSrtCues`. Also fixed a related production bug found while proving this for real: an existing-but-empty captions file used to crash the whole ffmpeg render ("Unable to open ... Error initializing filters") instead of failing the new, nameable QA check — `tools/media.ts` now treats a 0-byte captions file the same as a missing one. `studio-real-ipc.test.ts` now drives real `media_narrate` (real TTS, real captions) instead of injected synthetic fixtures, plus a real flat-placeholder negative case; `media-render.live.test.ts` gained matching positive/negative real-ffmpeg cases; both are now wired into `nightly-media-e2e.yml` via `HOMEBOT_LIVE=1` so the full chain runs on a schedule, not just ad hoc. Extended the `homebot:media:run` IPC bridge (preload + shared types) to pass through `image`/`visuals` overrides — needed for the negative real-IPC test, and a reasonable general capability the panel didn't have before. Windows: tsc 0 errors, eslint 0 errors, full suite 4015/4015. Live verification (not just unit-mocked, run once against this exact code before the rebase): `HOMEBOT_LIVE=1` run — real narrated render passes all new gates, flat placeholder rejected, empty captions rejected, real scene-image render passes (the file's pre-existing "ffmpeg missing" test fails only on this dev machine, which has a real ffmpeg at `C:\ffmpeg\bin\ffmpeg.exe` — one of `findFfmpeg`'s hardcoded fallback paths, unrelated to PATH/env hiding and not present on a fresh CI runner using choco). `HOMEBOT_E2E_FFMPEG=1` real-IPC run: both the real-narration positive case and the flat-placeholder negative case pass over the actual Electron IPC boundary. Supersedes PR #293 (identical code, closed for the CLAIMS.md-only conflict). |
| ~~Track D — Free-setup guidance & actionable fixes in Capability Doctor~~ | claude/track-d-capability-doctor-free-setup | ~~In progress (2026-09-12)~~ → **MERGED as #295 (2026-09-12)** | Elevate Capability Doctor with actionable 1-click in-app navigation and copyable winget/CLI setup commands for missing and unconfigured free engines (Ollama, recommended models, Git, FFmpeg, n8n, Qdrant). |
| ~~Track C — Capability Doctor truth across all three headline pillars~~ | claude/track-c-capability-doctor-pillars | ~~In progress (2026-09-12)~~ → **MERGED as #291 (2026-09-12)** | Expand capability report to probe and report ground truth for Local Image Diffusion (sd-cpp engine + model) and Coding Tools (git), completing coverage across Assistant, Studio, and Code. |
| ~~Track F — Code Mode front door in mode bar & dashboard navigation~~ | claude/track-f-code-mode-front-door | ~~In progress (2026-09-12)~~ → **MERGED as #290 (2026-09-12)** | Elevate Code to primary mode position with dedicated code glyph; add Code Workspace quick action to Dashboard; wire WorkspaceShell status-bar Home navigation. |
| ~~Audio true-peak headroom fix — satisfy <= -1.5 dBTP ceiling on lossy AAC renders~~ | claude/media-render-true-peak-headroom | ~~Ready for integration~~ → **MERGED as #286 (2026-09-11)** | Fixes +0.17 dB true peak overshoot caused by AAC 128k MDCT inter-sample peak regrowth. Targets -2.0 dBTP in loudnorm (per EBU R128 s2 / AES TD1004 lossy codec delivery guidelines), ensuring measured post-encode True Peak strictly satisfies the <= -1.5 dBTP ceiling (-1.77 dBTP measured) while maintaining -16.1 LUFS integrated loudness. |
| ~~Local sd.cpp GPU serialization + configurable scene concurrency~~ | claude/media-visuals-local-concurrency | ~~Ready for integration~~ → **MERGED as #285 (2026-09-11)** | Serialize local sd-cpp diffusion generation (concurrency 1) to prevent 4GB VRAM thrashing and timeouts; retain concurrency 4 for parallel network APIs; add process-level mutex queue in trySDCpp. |
| ~~sd.cpp explicit GPU device selection + local-generation-aware scene timeout~~ | claude/sdcpp-gpu-device-selection | ~~Ready for integration~~ → **MERGED as #284 (2026-09-11)** | Root cause of the ~910s local-generation number in the owner's own architecture-decision discussion: `sd-cli.exe`'s `--auto-fit` reports "no GPU devices" on this exact machine (i5-12450H + RTX 2050) and silently runs on CPU, taking 910.74s of sampling then failing at VAE-decode with zero images produced. `--list-devices` on the identical binary finds both GPUs immediately (RTX 2050 with `NV_coopmat2`) — auto-fit's detection is broken, not its placement. `pickSDCppDevice()` runs `--list-devices` once (cached per process), prefers a discrete-GPU name, passes `--backend diffusion=X,vae=X,clip=X` explicitly; falls back to auto-fit's own default if none found. Also raises `SCENE_TIMEOUT_MS` 15s→60s (`claude/media-visuals-sdcpp-fallback`/#282's 15s value was tuned for network generation alone and would have killed every local generation before completion — 21–37s measured even with the GPU fix). Fixes `image-generate-tool.test.ts`'s missing `child_process` mock, exposed once a real local install existed on this machine. Verified twice through the real production call chain (not a raw binary call): `imageGenerateHandler` direct 37.0s, `generateSceneImages` end-to-end 21.1s, both real images inspected visually. Windows: tsc 0 errors, eslint 0 errors, full suite 3991/3991. Supersedes #283 (same fix, closed — branched before #282's squash-merge, git saw a phantom conflict on identical lines). |
| ~~Media visuals — direct local sd-cpp wiring, parallel scene calls, timeout fallback, 4GB GPU plan amendment~~ | claude/media-visuals-sdcpp-fallback | ~~Ready for integration~~ → **MERGED as #282 (2026-09-11)** | Direct `trySDCpp` wiring in `generateSceneImages` (skips offline port probing), CONCURRENCY=4 for parallel scene dispatch, 15s timeout with automatic `generateFallbackPlate` fallback, cold-cache end-to-end measured at 24.70s (down from 157.6s), amended `MEDIA_STUDIO_PLAN.md` 4GB GPU diffusion bounds for quantized SD-1.5/LCM. |
| ~~Real-IPC/real-userData Media Studio e2e test + nightly gate~~ | claude/real-ipc-nightly-gate-v3 | ~~Ready for integration~~ → **MERGED as #288 (2026-09-11)** | `studio-real-ipc.test.ts` runs Playwright Electron against disposable test userData sandbox (zero APPDATA pollution) and managed FFmpeg, asserting structured-clone fidelity and ffprobe stream metadata. Gated behind `HOMEBOT_E2E_FFMPEG=1`; `nightly-media-e2e.yml` runs daily at 02:00 UTC. Third and final attempt (originally #281, rebased as #287, rebased again as #288 after #286 merged and put #287 `BEHIND` main) — both rebases were pure `git merge-tree`-confirmed CLAIMS.md-only conflicts, code applied cleanly each time. Content verified live on main post-merge (`git show origin/main:...` on both files, plus `git merge-base --is-ancestor` on the squash-merge SHA).
| ~~Scene-image prompt-hash caching + per-scene generation timeout~~ | claude/scene-image-cache-timeout | ~~Ready for integration~~ → **MERGED as #280 (2026-09-11)** | Root cause: 143.89s wall clock was imageGenerateHandler walking up to 6 providers sequentially per scene. Adds SHA-256 prompt-hash caching (`sceneCacheKey`) with opt-in `cacheDir` and 45s per-scene generation timeout. |
| ~~Media render hardening — BT.709 color pinning + two-pass loudness normalization~~ | claude/media-render-bt709-loudnorm | ~~Ready for integration~~ → **MERGED as #279 (2026-09-11)** | Forces `-pix_fmt yuv420p -color_range tv -color_primaries/trc/colorspace bt709` at scaler and encoder, plus matching `-x264-params`. Adds real two-pass `loudnorm`: `measureAudioLoudness` runs ffmpeg pass 1 against mixed/ducked audio, `buildLoudnormFilter` injects measured stats into linear pass 2 after `amix`. |
| ~~Task 2 — Media Studio IPC transition boundary regression coverage~~ | claude/task2-studio-ipc-tests | ~~Ready for integration~~ → **MERGED as #277 (2026-09-10)** | `widget/src/main/__tests__/studio-gateway-ipc.test.ts` exercises the real guard chain (`registerBundledStudioIpc` → `StudioIpcGuard` → `applyMediaTransition`) for `homebot:media:advance`/`approve`/`reject`. |
| ~~DeepSeek model discovery — show current models for a DeepSeek key~~ | claude/deepseek-model-discovery | ~~Ready for integration~~ → **MERGED as #274 (2026-09-09)** | Calls DeepSeek `/models` with saved key; preserves API ids, surfaces source, gated via `assertProviderOnlineAccess`. |
| ~~Codex front door — make the built ChatGPT-subscription provider selectable, and probe the ffmpeg HomeBot installed~~ | claude/codex-front-door-ffmpeg-probe | ~~Ready for integration~~ → **MERGED as #276 (2026-09-10)** | Added codex option in `CloudProviderSection.tsx`, switched CLI-path to `isSubscriptionCli`, and updated `capability-probe.ts` to check `findManagedFfmpeg()`. |
| HB-M2 prerequisite — narration online consent | claude/studio-speech-privacy | Claimed in another active session — Codex 2026-09-09 | Existing `.kilo/worktrees/codex-speech-privacy` owns shared speech/Edge/Kokoro privacy; observed active claim and uncommitted speech tests. Preserve this work. |
| ~~Narrate clip bug fix — pass video path to Python analyzer~~ | claude/provider-correctness | ~~Ready for integration~~ → **MERGED as #272 (2026-09-10)** | Fixed `narrate-clip.ts:196` where `buildAnalyzerArgs(analyzerScript, analysisOut)` was called instead of `[analyzerScript, ...buildAnalyzerArgs(videoPath, analysisOut)]`. The video path was missing, causing the Gemini clip analyzer to receive the script path as input. All 9 tests pass (verified locally); tsc and eslint clean. Reachable via `media_narrate_clip` registered by `modules/bundled/studio.ts`. |
| ~~Chat → agent bridge proof: Gemini dynamic model discovery~~ | claude/gemini-model-discovery | ~~Ready for integration~~ → **consolidated into PR #274 (2026-09-10)** | Replace the static `GOOGLE_AI_MODELS` / `GOOGLE_GEMINI_NATIVE_MODELS` reliance with dynamic discovery via Google's `models.list` endpoint when a key is present; filter to `generateContent`-capable models, keep `supportedGenerationMethods` as separate `capabilities` metadata, cache briefly, fall back to the small static list on failure. Never infer account tier/entitlement from the list. |
| HB-M2 prerequisite — narration online consent | claude/studio-speech-privacy | Claimed in another active session — Codex 2026-09-09 | Existing `.kilo/worktrees/codex-speech-privacy` owns shared speech/Edge/Kokoro privacy; observed active claim and uncommitted speech tests. Preserve this work. |
| ~~Ancient Pathways Integration — run video workflow from Media Studio UI & chat sprite generation~~ | claude/ancient-pathways-integration | ~~Ready for integration~~ → **MERGED as #248 (2026-09-05)** | Media Studio episode showcase with 9 episodes, season filter, real-time search, 4-step progress stepper (Story -> Voices -> Animation -> 1080p Video), celebratory 1080p MP4 preview player. Autonomous Sprite Pipeline (`media_generate_sprites`) generates 8-panel model sheets via Imagen 3/Pollinations, auto-isolates background, slices via Python, outputs `manifest.json`. Verified: widget tsc 0 errors; eslint 0 errors; 76/76 tests green; docs:check in sync. |
| ~~duplicate-export-guard runs on push, not just pull_request~~ | claude/review-o-x-alpha-commits-1qrn25 | ~~Ready for integration~~ → **MERGED as #244 (2026-09-04)** | One line in `ci.yml`. `auto-merge.yml` opens PRs as github-actions[bot]; GitHub attributes that pull_request event to the bot and holds EVERY workflow on it at action_required - six held runs on one commit of #215, measured. Push-triggered runs are attributed to the pusher and run fine, but this job was gated `if: github.event_name == 'pull_request'`, so on those it reported "skipped" and never "success". A one-commit PR nobody touches again waits forever on an approval nobody knows to give - the "ten runs held, five for three days" note. Gate removed; base ref is now `github.base_ref \|\| 'main'`. All five required checks are now satisfiable from push runs alone, so a `claude/**` branch can auto-merge unattended. Verified BOTH directions with an empty base ref (a zero from this check is otherwise indistinguishable from a broken one): a deliberate colliding export exits 1 and names the file, a clean tree exits 0. Also records the live `WorkspaceShell.tsx:83` instance of the whole-effect handoff guard in `renderer-ui.instructions.md` - unfixed, track F, nobody's claim. Does NOT touch product code. |
| Model picker sections  purpose-grouped catalog, GPU-picked float-up, type-to-filter | claude/model-picker-sections | Merged as #249 (2026-09-04) | Research basis: VS Code groups recommended-first, Raycast/Cursor group by task, all ship type-to-filter. Adds explicit category tags to the 17-model catalog (everyday / coding / reasoning / lightweight / uncensored), an On this PC section with a Best for your PC float-to-top subsection driven by detected VRAM, purpose sub-groups under the download section, a filter box with Escape-to-close, and no-match state. Arrows cycle the filtered subset. Tests 41/41. |
| Video engine decision recorded  JSON API first, Remotion deferred | claude/model-picker-sections | Superseded 2026-09-08 | Historical rationale retained in `docs/VIDEO_ENGINE_DECISION.md`. The approved `docs/MEDIA_STUDIO_PLAN.md` now prioritizes CPU FFmpeg, assisted Colab and consented online providers; new paid JSON rendering is deferred. |
| Chat avatar excitement pass  visible glowing orb avatars + atmosphere polish | claude/ui-excitement-pass | Merged via #243/#246/#249 | Stacked on claude/visual-pass (#243). Discovery: #243's orb CSS was dead code  MessageBubble.tsx still rendered the old PNG badges (`HomeBotChatAvatar.png`/`UserChatAvatar.png`) inside `.message-avatar` at `width/height: 100%; object-fit: cover`, completely covering the orb gradient. This pass replaces the PNG `<img>` with pure CSS orbs + Icon-set glyphs (sparkle for HomeBot, user for you), adds a rotating conic shimmer, specular highlight, streaming-state-reactive pulse (wrapper already carries `data-state`), aurora grain overlay, and selection/focus polish. Reduced-motion respected throughout. Dead avatar and logo PNGs removed (~3MB). New e2e spec asserts the orbs in the real renderer (circles, decorative layers, grain below overlays). |
| ~~Track F — per-field handoff guard in `WorkspaceShell.tsx:83` (whole-effect guard silently drops a second chat handoff into a panel the user is already looking at)~~ | claude/track-f-handoff-guard | ~~Ready for integration~~ → **MERGED as #252 (2026-09-05)** | Split the root check from navContext apply so that re-running the effect with a fresh navContext while root is already set still opens the targeted file. Pattern from AutomationCenter.tsx:131-133. Adds a renderer test for handoff onto already-rooted shell. Verified: widget tsc 0 errors; eslint 0 errors; 731 tests green. |
| ~~Narrate my clip  BYO video narration inside HomeBot (chat-reachable)~~ | claude/narrate-my-clip | ~~Ready for integration~~ → **MERGED as #242 (2026-08-26)** | Aden redirected the NBA pipeline: he wants it in the app, not in PowerShell/Drive. New native tool `media_narrate_clip` — give it a video path in chat, it runs the Gemini vision scripter, synthesizes narration through the EXISTING engine seam, muxes onto the original clip with ffmpeg (-c:v copy). Home-boundary enforced on input and output; fails closed with plain setup guidance when no Gemini key. Verified: 242 suites / 3461 tests green. Worktree: Desktop/sadie-nba3. **Retired 2026-09-04: PR #242 verified MERGED (2026-08-26 12:48 UTC) - this row sat "Ready for integration" for nine days after merging, the exact rot rule 4 describes.** |
| ~~Ancient Pathways pipeline doctor integration — expose quality checks in Media Studio~~ | claude/ancient-pathways-integration | ~~Ready for integration~~ → **MERGED as #255 (2026-09-05)** | Media Studio episode showcase with 9 episodes, season filter, real-time search, 4-step progress stepper (Story -> Voices -> Animation -> 1080p Video), celebratory 1080p MP4 preview player. Autonomous Sprite Pipeline (`media_generate_sprites`) generates 8-panel model sheets via Imagen 3/Pollinations, auto-isolates background, slices via Python, outputs `manifest.json`. Includes `media_measure_mouth_anchors` tool bridging to AP's `learn_from_anchors.py`, `runDoctorChecks` with `homebot:media:ancient-pathways-doctor` IPC handler, and `mediaAncientPathwaysShowrunner` bridging to AP's `/api/production/generate`. Verified: 242 suites / 3521 tests green (15 skipped). **Also: harden AP server-side repair loop, render lock, and doctor checkpoint (committed to AP master)** — narrow `REPAIRABLE_CHECKS` allowlist in `evaluator.py`, `acquire_lock`/`release_lock` spanning background job lifetime in `run_showrunner.py`, doctor.py post-compilation checkpoint. 196 AP tests green (3 xfailed). |
| ~~Ancient Pathways pipeline refinement — shot scale gates, dead scripts, studio mouth anchors~~ | claude/ap-pipeline-refinement | ~~COMPLETED~~ → **MERGED as #254 (2026-09-05)** | Integration work added in this PR. The AP project already has G4/M3 shot scale checks enhanced, dead scripts locked with xfail, and 8 studio mouth anchors documented. This PR adds the `media_measure_mouth_anchors` tool bridging to AP's `learn_from_anchors.py`, exposing quality checks through Media Studio UI, plus 7 unit tests, permission registration, and permission-copy entry. Verified: 242 suites / 3521 tests green (15 skipped). |
| Codex front door — make the built ChatGPT-subscription provider selectable, and probe the ffmpeg HomeBot installed | claude/codex-front-door-ffmpeg-probe | Ready for integration — **PR open (2026-09-11)** | Two reachability defects on `b279e87`, both verified honesty A/B (tests fail on main, pass with the fix). (1) The `codex` provider was fully built and unit-tested — `streamCodex`, `CODEX_MODELS`, `codex` in the settings type union, in `KEYLESS_PROVIDERS`, in `curatedProviders`, deliberately absent from `PROVIDER_API_URLS`, handled by name in IPC and `discovery-payload` — but `CloudProviderSection.tsx` had no `<option value="codex">`, so it could never be chosen and never ran. Every downstream gate was already generic; the dropdown was the sole missing link. Adds the option, switches the CLI-path input from `isClaudeCode` to the already-exported `isSubscriptionCli` (codex is keyless — it was getting a password box and no way to set its path), and adds a Codex hint. (2) `capability-probe.ts` probed only PATH for ffmpeg while Media Studio prefers the managed copy in userData, so Home reported "ffmpeg: not installed" on machines that were rendering fine; now falls back to `findManagedFfmpeg()` and runs it. Verified Windows: widget tsc 0 errors, eslint 0 errors on changed files, full widget suite 3,949 passed / 0 failed (283 suites, 15 pre-existing skips). New tests assert the real effect — the option's presence, the saved config being `resolveCloudLLM`-active with no key, and the managed binary actually being executed — with negative controls both sides. No live Codex CLI or live render was exercised. |

---

## Retired claims

| Feature | Branch | Status | Notes |
|---|---|---|---|
| ~~HB-M2 — reachable Studio module controls~~ | claude/studio-module-controls | Implementation verified; build claim released — Codex 2026-09-09 | PR #269 includes movie privacy #266 and image artifacts #267. Final Windows checkpoint: 3,867 widget tests (15 existing skipped), all 53 real Electron tests without retries, and preceding unchanged root 227 tests. Typechecks/build/lint/docs/import checks pass. Disable removes navigation/tools and blocks IPC; restart retains the choice/files; enable restores one copy. The measured startup-toast obstruction is fixed and covered by a failing-before/passing-after navigation regression; three fresh profiles open Studio in 386/211/463 ms, within M0 time/memory limits. Evidence and raw before/after samples: docs/STUDIO_MODULE_CONTROLS.md. Current required CI, merge status and integration ownership: https://app.notion.com/p/3d5829ebf7be814eaccceeaaa9b1e565. Full M2/media acceptance remains open. |
| ~~HB-M2 prerequisite — durable movie image outputs~~ | claude/studio-movie-artifacts | **MERGED as #267**, verified 2026-09-09 local | Main 4b9cdf2. Four image adapters save readable shot images; router and runner reject unusable results and corrupt cached/deferred images. Windows: 3,850 widget + 226 root tests, static/build/docs checks and four actual Electron image/privacy tests pass. All six required contexts are green. Evidence: `tasks/movie-image-output.md`. No live AI provider, video QC or full M2 claim. |

Movie/image provider online consent is **merged as #266**, confirmed on
2026-09-09 local time as main `19535cb`. Local Windows: 3,827 widget and 226
root tests, typechecks/lint/build/docs and real Electron movie denial pass.
No provider requests occur when Online is off; controlled loopback generation
remains usable. Evidence: `tasks/movie-provider-privacy.md`. This is privacy
and saved-state evidence; live-provider quality remains unproven. Main exactly
matches tested head `bbb5202`; remote evidence is also recorded in Notion.

HB-M1 is **merged as #265**, confirmed on 2026-09-09 local time. Main
`1e3b064` exactly matches tested head `3e08059` (tree `d6f7483`). All six required
contexts are green; Windows CI ran 3,785 widget tests, 226 root tests and 13
overlay tests. The Electron matrix ran 16 + 16 + 16 tests on Windows; all nine
shards passed. One macOS install network reset recovered on an isolated rerun
that executed 16 tests. Evidence: `docs/CORE_MODULE_CONTRACT.md` and PR #265.

| Feature | Branch | Status | Notes |
|---|---|---|---|
| ~~Media QA output trust~~ | claude/media-qa-trust-fix | **MERGED as #264 (2026-09-08 UTC), complete** | Main `eafd27f` exactly matches the tested `3c26e12` tree. Unavailable/failed QA persists needs_revision with output/source files retained; approval remains blocked. Three regression cases; widget 3,685/root 219 tests. All six required contexts green; Windows matrix 16 + 16 + 15 actual tests on first attempts, application CI 3,685 unit + 13 overlay tests. Mocked media adapters do not prove real video quality. Evidence: tasks/provider-output-trust.md. |
| ~~HB-M0 / media Task 1 — baseline reconciliation and module map~~ | claude/studio-m0-baseline (via #259) | **MERGED as #259 (2026-09-08), complete** | Main `8327342` has the exact tested tree `5b45be1`; all six required checks present and green. Root 219, widget 3,682 and Windows matrix 47 actual tests passed; local full Electron 47 passed without retries. Three fresh-profile measurements and source-path map in `docs/CORE_MODULE_BASELINE.md`. One Linux dependency-download failure recovered with an isolated rerun that executed 16 tests. |
| ~~Media studio follow-up — movie/storyboard integration~~ | claude/ancient-pathways-integration | **MERGED as #259 (2026-09-08)** | Storyboard/director/FFmpeg renderer, ComfyUI adapter, timeline/DCC workspace and feed-library work landed with #260/#262 retained. Generated root movie data is excluded from Git and retained locally. Provider/privacy, real-media output, editing and packaging acceptance remain open. |

Windows E2E repair: `claude/studio-baseline-ci` is **merged as #262**, confirmed
2026-09-08 at `0c85650`. All six required contexts were present and successful;
the Windows matrix ran 16 + 16 + 15 actual Electron tests. The launcher defect
is closed; Studio integration subsequently merged in HB-M0 above.

Movie baseline audit (2026-09-08): the three former active rows naming #258
for router wiring, five adapters and the project runner are retired. Those
modules exist on main at `6b95e5e`, and #258 is merged. Their earlier claims of
working zero-cost, character-consistent production were not established by
the audit. #259 subsequently merged under HB-M0; code presence is not video proof.

| Feature | Branch | Status | Notes |
|---|---|---|---|
| ~~TTS A/B measurement — Kokoro vs Edge performance comparison~~ | claude/tts-ab-measure | **DOCUMENTED** | Complete A/B measurement comparison in PR #253. Edge: 505ms-1.1s render, 96kbps MP3, smaller files. Kokoro: 25-50s render (includes 1.5-65s model load), 768kbps WAV, 10x larger files. Recommendation: Edge for web/energy, Kokoro for high-quality offline. |
| ~~Media studio baseline — overlay regression and audit documentation~~ | claude/studio-baseline | Merged as #260 (2026-09-08), content verified on main 9ec3ebd | Both new entrance tests failed before and passed after; 3571 widget tests, 213 root tests, 13 local overlay/tooltip E2E passed. macOS CI shard 2 actually ran 16 tests and passed. Windows matrix's green was false: zero tests, null exit; the separate active CI claim repairs that. |
| ~~duplicate-export-guard runs on push, not just pull_request~~ | claude/review-o-x-alpha-commits-1qrn25 | ~~Ready for integration~~ → **MERGED as #244 (2026-09-04)** | One line in `ci.yml`. `auto-merge.yml` opens PRs as github-actions[bot]; GitHub attributes that pull_request event to the bot and holds EVERY workflow on it at action_required - six held runs on one commit of #215, measured. Push-triggered runs are attributed to the pusher and run fine, but this job was gated `if: github.event_name == 'pull_request'`, so on those it reported "skipped" and never "success". A one-commit PR nobody touches again waits forever on an approval nobody knows to give - the "ten runs held, five for three days" note. Gate removed; base ref is now `github.base_ref \|\| 'main'`. All five required checks are now satisfiable from push runs alone, so a `claude/**` branch can auto-merge unattended. Verified BOTH directions with an empty base ref (a zero from this check is otherwise indistinguishable from a broken one): a deliberate colliding export exits 1 and names the file, a clean tree exits 0. Also records the live `WorkspaceShell.tsx:83` instance of the whole-effect handoff guard in `renderer-ui.instructions.md` - unfixed, track F, nobody's claim. Does NOT touch product code. |
| ~~Model picker sections — purpose-grouped catalog, GPU-picked float-up, type-to-filter~~ | claude/model-picker-sections | Merged as #249 (2026-09-04) | Research basis: VS Code groups recommended-first, Raycast/Cursor group by task, all ship type-to-filter. Adds explicit category tags to the 17-model catalog (everyday / coding / reasoning / lightweight / uncensored), an On this PC section with a Best for your PC float-to-top subsection driven by detected VRAM, purpose sub-groups under the download section, a filter box with Escape-to-close, and no-match state. Arrows cycle the filtered subset. Tests 41/41. |
| ~~Video engine decision recorded — JSON API first, Remotion deferred~~ | claude/model-picker-sections | Superseded 2026-09-08 | Historical rationale retained in `docs/VIDEO_ENGINE_DECISION.md`. The approved `docs/MEDIA_STUDIO_PLAN.md` now prioritizes CPU FFmpeg, assisted Colab and consented online providers; new paid JSON rendering is deferred. |
| ~~Chat avatar excitement pass — visible glowing orb avatars + atmosphere polish~~ | claude/ui-excitement-pass | Merged via #243/#246/#249 | Stacked on claude/visual-pass (#243). Discovery: #243's orb CSS was dead code - MessageBubble.tsx still rendered the old PNG badges (`HomeBotChatAvatar.png`/`UserChatAvatar.png`) inside `.message-avatar` at `width/height: 100%; object-fit: cover`, completely covering the orb gradient. This pass replaces the PNG `<img>` with pure CSS orbs + Icon-set glyphs (sparkle for HomeBot, user for you), adds a rotating conic shimmer, specular highlight, streaming-state-reactive pulse (wrapper already carries `data-state`), aurora grain overlay, and selection/focus polish. Reduced-motion respected throughout. Dead avatar and logo PNGs removed (~3MB). New e2e spec asserts the orbs in the real renderer (circles, decorative layers, grain below overlays). |
| ~~Visual pass merged as #233 (2026-08-25, content verified on main)~~ | claude/visual-pass | Merged (2026-08-25, content verified on main) | Premium dark desktop AI theme with indigo-violet accent, glassmorphism, and modern UI polish. |
| ~~NBA voiceover pipeline merged as #236 (2026-08-26)~~ | claude/nba-voiceover | Merged (2026-08-26) | Integrated XTTS clone and voice processing into NBA analysis pipeline. |
| ~~Narrate my clip — BYO video narration inside HomeBot (chat-reachable)~~ | claude/narrate-my-clip | ~~Ready for integration~~ → **MERGED as #242 (2026-08-26)** | Aden redirected the NBA pipeline: he wants it in the app, not in PowerShell/Drive. New native tool `media_narrate_clip` — give it a video path in chat, it runs the Gemini vision scripter (`scripts/analyze_clip.py`, key read through `apiKeyForProvider(settings,'google-ai-studio')`, never a second lookup), synthesizes narration through the EXISTING engine seam (`renderNarrationToFile`), muxes onto the original clip with ffmpeg (-c:v copy) and hands back the final path beside the source. Home-boundary enforced on input and output; fails closed with plain setup guidance when no Gemini key; analyzer key travels via env, never argv. Verified in sadie-nba3 (own npm ci, not the pnpm-touched shared tree): tsc clean; eslint 0 errors; new suite 13 tests incl. full mocked happy-path asserting key-in-env and copy-only mux args; permission-copy registry entry added (gate caught it - that is what gates are for); tool-permissions-parity passes; full serial widget suite 242 suites / 3461 tests green; root 213 green. Does NOT touch media-studio state machine, voice.ts internals, or message-router routing logic. Colab/XTTS clone remains rung 2 behind the existing engine picker. Worktree: Desktop/sadie-nba3. **Retired 2026-09-04: PR #242 verified MERGED (2026-08-26 12:48 UTC) - this row sat "Ready for integration" for nine days after merging, the exact rot rule 4 describes.** |

## 2026-09-08 — approved media baseline (Codex)

- `docs/MEDIA_STUDIO_PLAN.md` is Aden's approved eight-task media execution order.
  Task 1 is authorized; ask before Task 2 and every later task. The 2026-09-04
  JSON API rendering decision is superseded for this baseline: CPU FFmpeg,
  assisted Colab for heavy generation, consented online providers, no automatic
  paid fallback. Preserve Ancient Pathways' separate animation plan.
- CI now runs the full Windows widget Jest suite, typecheck, build and overlay
  E2E. The three-OS matrix's `e2e-all` is required, not advisory. Re-read live
  protection and assert every required context is present and green.
- Portalling a backdrop is necessary but not sufficient: `ambientPanelIn`
  scaled the entire Analytics/permission overlay to 98%, exposing the window
  edges during entrance. Keep backdrop geometry fixed; animate the inner card.
  The regression samples actual CSS animations and edge hit-testing at 0%,
  50% and 100%, instead of waiting longer until the defect disappears.

## 2026-09-08 — continuous Notion plan tracking (Aden's instruction)

Aden asked Codex to keep working through the Notion plan, update Notion after
every verified success, and check whether the plan has changed. This supersedes
the earlier per-task confirmation cadence for already-authorized development;
publication, credentials/accounts, payments and destructive actions keep their
existing boundaries. The existing Notion `Codex Autonomous Build Loop` now
contains this rule. Local skill: `~/.codex/skills/homebot-notion-progress/SKILL.md`.
Fetch the live plan before each task and before each targeted progress update;
preserve concurrent edits and confirm asynchronous writes succeeded.

## Known non-duplicates

These identifiers are exported by multiple files intentionally (same name, different domains). The duplicate-export guard is suppressed for these:

- `MovieRoutingDecision` — exported by `widget/src/main/movie/types.ts`; distinct from `RoutingDecision` in `widget/src/main/message-router.ts`. Different domains: movie production routing vs. message/intent routing.

## 2026-09-08 — locked M2 kickoff and HB-M0 integration

Aden's instruction to follow the Notion plan starts HB-M0 under the locked
Core + Modules / Media Studio M2 plan. Its current authority and concrete
existing seams are in `docs/CORE_MODULE_BASELINE.md`; media acceptance gates
remain in `docs/MEDIA_STUDIO_PLAN.md`. Wrap the widget's actual tool Map and
the Studio IPC paths, not only the separately tested root ToolRegistry.
The next host work must preserve existing jobs, assets, privacy and approval
authority. No new framework, paid provider, production publication or sales
is part of this baseline. `/movie/` is ignored as local project/run data;
the pending sample was removed from Git only and retained on disk.

## 2026-09-09 — movie provider dispatch consent

Movie/image adapters check the existing Online choice at their main-process
entry points, including direct helpers and every remote ComfyUI request.
Configured local-provider URLs can be remote: only loopback bypasses consent,
not private LAN addresses. Reuse `utils/provider-network-policy.ts` for this
HTTP boundary; request arguments cannot grant Online access. A movie report
with failed shots now returns an IPC failure and leaves full reasons in the
report/log. Before/after, local transport and real Studio UI evidence is in
`tasks/movie-provider-privacy.md`; remaining M2 acceptance is still open.

## 2026-09-09 - YouTube connection implementation ready (Codex)

The bounded owner-requested connection is implemented on claude/youtube-desktop-connection. Connect -> YouTube imports Desktop app JSON, uses system-browser PKCE/state OAuth, stores its grant through Core OS encryption, and checks channel identity. No upload or publishing capability was added. The build claim is released; remote required checks and merged-content verification remain integration conditions, tracked at https://app.notion.com/p/3d6829ebf7be81f3a9f2c96656655dc9.

Windows checks: 3,903 widget tests and 227 root tests pass; both typechecks, lint (zero errors/eight existing warnings), build, docs and import/export guards pass. Two rebuilt Electron tests pass in 1.2 minutes without retries, covering the YouTube flow with synthetic Google responses and real Windows encrypted storage, restart/refresh/removal/corrupt-grant recovery, plus existing module controls. A rejected replacement sign-in preserves the previous grant; two regression cases failed before that correction and pass afterwards. Test paths, setup steps and live-account limits are in docs/YOUTUBE_CONNECTION.md. Live owner OAuth and key rotation remain unverified owner actions. Existing speech, local-export and lazy-runtime claims are preserved.
