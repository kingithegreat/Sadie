// Stub for static file imports (PNG, JPG, SVG, etc.) in Jest tests.
// Returns a plain string so components that use the URL still render without crashing.
const fileMock = 'test-file-stub';
export default fileMock;
